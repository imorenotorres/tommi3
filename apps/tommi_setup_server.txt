# TOMMI Server Setup — Configuration Variables
# Fill in the values and pass this file to setup.sh:
#   ./apps/setup.sh apps/tommi_setup_server.txt
#
# When this file is provided, setup.sh runs without prompting.

# 1. Conversation logging (y/n)
#    Recommended: y (needed for accountability under GDPR Art. 5(2))
ENABLE_LOGGING=y

# 2. LLM provider API key (leave empty if using only Ollama)
#    Get it at: https://console.mistral.ai/api-keys
MISTRAL_API_KEY=

# 3. Administrator account
#    Password requirements: min. 8 characters, at least one uppercase letter,
#    one lowercase letter, one digit, and one special character (!@#$%...)
ADMIN_USER=admin
ADMIN_PASSWORD=

# 4. SMTP configuration (for user invitation emails, optional)
#    For Gmail: use smtp.gmail.com with an App Password
#    (https://myaccount.google.com/apppasswords)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_TLS=true
